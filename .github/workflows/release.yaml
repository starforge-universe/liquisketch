name: 'Release'

on:
  push:
    branches:
      - 'release/*.*.x'


jobs:
  # Extract version from branch name
  extract-version:
    name: 'Extract Version'
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.patch.outputs.version }}
    steps:
      - name: 'Checkout project sources'
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Extract major/minor from branch name
        id: version
        run: |
          BRANCH="${GITHUB_REF##*/}"        # release/1.4.x → 1.4.x
          MAJOR=$(echo $BRANCH | cut -d. -f1)
          MINOR=$(echo $BRANCH | cut -d. -f2)

          echo "MAJOR=$MAJOR" >> $GITHUB_OUTPUT
          echo "MINOR=$MINOR" >> $GITHUB_OUTPUT
      
      - name: Determine next patch version
        id: patch
        run: |
          MAJOR=${{ steps.version.outputs.MAJOR }}
          MINOR=${{ steps.version.outputs.MINOR }}
  
          # Find existing tags like v1.4.*
          TAGS=$(git tag --list "v${MAJOR}.${MINOR}.*")
  
          if [ -z "$TAGS" ]; then
            PATCH=0
          else
            # Extract numeric patch values, sort, pick highest
            PATCH=$(echo "$TAGS" | sed -E "s/v${MAJOR}.${MINOR}.//" | sort -n | tail -1)
            PATCH=$((PATCH + 1))
          fi
  
          VERSION="${MAJOR}.${MINOR}.${PATCH}"
  
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Next version: $VERSION"

  # Run checks first
  checks:
    name: 'Run Checks'
    needs: [extract-version]
    uses: ./.github/workflows/__call__checks.yaml

  # Build package with extracted version
  # The build workflow will:
  # 1. Update pyproject.toml with the extracted version
  # 2. Build the package using the updated version
  # 3. Verify the built package has the correct version
  build:
    name: 'Build Package'
    needs: [extract-version, checks]
    uses: ./.github/workflows/__call__build.yaml
    with:
      version: ${{ needs.extract-version.outputs.version }}

  # Publish to Test PyPI
  publish-test:
    name: 'Publish to Test PyPI'
    needs: [build]
    if: github.ref_type == 'branch' && startsWith(github.ref_name, 'release/')
    uses: ./.github/workflows/__call__publish_pypi.yaml
    with:
      environment: 'test'
      python-version: '3.11'
      twine-repository-url: ${{ vars.PYPI_REPOSITORY || 'https://test.pypi.org/legacy/' }}
    secrets:
      pypi_username: ${{ secrets.PYPI_USERNAME }}
      pypi_password: ${{ secrets.PYPI_PASSWORD }}

  # Create release tag
  tag-release:
    name: 'Tag Release'
    needs: [extract-version, publish-test]
    runs-on: ubuntu-latest
    if: github.ref_type == 'branch' && startsWith(github.ref_name, 'release/')
    permissions:
      contents: write
    steps:
      - name: 'Checkout project sources'
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 'Create and push tag'
        run: |
          VERSION="${{ needs.extract-version.outputs.version }}"
          if [ -n "$VERSION" ]; then
            git config --local user.email "action@github.com"
            git config --local user.name "GitHub Action"
            git tag -a "v$VERSION" -m "Release version $VERSION"
            git push origin "v$VERSION"
          fi

  # Publish to Production PyPI
  publish-production:
    name: 'Publish to Production PyPI'
    needs: [tag-release]
    if: github.ref_type == 'branch' && startsWith(github.ref_name, 'release/')
    uses: ./.github/workflows/__call__publish_pypi.yaml
    with:
      environment: 'production'
      python-version: '3.11'
      twine-repository-url: ${{ vars.PYPI_REPOSITORY || 'https://upload.pypi.org/legacy/' }}
    secrets:
      pypi_username: ${{ secrets.PYPI_USERNAME }}
      pypi_password: ${{ secrets.PYPI_PASSWORD }}
